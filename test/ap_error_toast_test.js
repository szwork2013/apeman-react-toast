/**
 * Test case for apErrorToast.
 * Runs with mocha.
 */
"use strict";

const apErrorToast = require('../lib/ap_error_toast.js'),
    assert = require('assert');

describe('ap-error-toast', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Ap error toast', (done) => {
        done();
    });
});

