/**
 * Test case for apWarnToast.
 * Runs with mocha.
 */
"use strict";

const apWarnToast = require('../lib/ap_warn_toast.js'),
    assert = require('assert');

describe('ap-warn-toast', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Ap warn toast', (done) => {
        done();
    });
});

