/**
 * Test case for apInfoToast.
 * Runs with mocha.
 */
"use strict";

const apInfoToast = require('../lib/ap_info_toast.js'),
    assert = require('assert');

describe('ap-info-toast', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Ap info toast', (done) => {
        done();
    });
});

